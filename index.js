const config = {
  printWidth: 120,
  trailingComma: "all",
  singleQuote: true,
  singleAttributePerLine: true,
  singleQuote: true,
  plugins: ["prettier-plugin-tailwindcss"],
};

export default config;
